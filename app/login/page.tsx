'use client';
import {useState} from 'react';
import {ArrowRight,ShieldCheck,LoaderCircle} from 'lucide-react';
export default function Login(){const [busy,setBusy]=useState(false),[error,setError]=useState('');return <main className="foundation-login"><a href="/" className="login-brand"><img src="/brand/sdc-logo.png" alt="SDC"/><span>SDC <b>COMMAND</b></span></a><section><span className="foundation-eyebrow">CONNECTED OPERATIONS</span><h1>Your team.<br/>One secure workspace.</h1><p>Sign in with your individual SDC account. Access is determined by your assigned role and sites.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const f=new FormData(e.currentTarget);try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password')})});const d=await r.json() as {error?:string};if(!r.ok)throw Error(d.error||"Sign-in failed.");window.location.assign('/workspace')}catch(e){setError((e as Error).message)}finally{setBusy(false)}}}><label>Work email<input name="email" type="email" autoComplete="username" required placeholder="you@company.com"/></label><label>Password<input name="password" type="password" autoComplete="current-password" required/></label>{error&&<p role="alert" className="foundation-error">{error}</p>}<button className="button brand-navy" disabled={busy}>{busy?<LoaderCircle className="spin" size={18}/>:<>Sign in <ArrowRight size={18}/></>}</button></form><small><ShieldCheck size={15}/> Accounts are issued by your administrator.</small></section><footer>SDC Security & Facility Services · Bengaluru</footer></main>}
